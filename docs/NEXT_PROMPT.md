# Next development prompt

Continue from the delivered Binternet 2026.09.08.4 source. Read AUDIT.md and docs/PAGINATION_FIX.md first. The operator confirmed .3 working on the VPS; .4's repaired continuation parser and optional infinite scrolling still need the documented Docker, live Pinterest and real-browser checks. Run the Docker build/runtime suite and the normal deployment candidate checks on a host with Docker/network access. Require an architecture search with a real next-page link and distinct new images on the second page. Do not bypass that gate to treat a failed search as a successful repair.

Verify Manual pages with JavaScript disabled, then select Infinite scroll and exercise successive pages, pause/resume, explicit retry, provider end markers, repeated cursors, duplicate-only responses and slow or refused requests. Check keyboard navigation, status announcements, responsive gallery reading order and preference preservation with real images. Measure browser memory during a long session: there is no fixed page ceiling, but retained cards and deduplication sets grow. Use those measurements before deciding whether bounded retention or an explicit session control is needed. Keep manual navigation available and load only the existing self-hosted script under the same-origin CSP.

Measure cold/warm searches, responsive image transfer sizes and concurrent image-proxy memory on the IONOS VPS; use those measurements to tune FPM limits and caches. Inspect only the affected NPM proxy host and confirm stable routing across candidate cutover and rollback. Extend deterministic coverage for any observed upstream response schema changes, TLS/DNS failure handling and stale image behavior. Preserve the license, attribution, hardened requests and deployment rollback. Produce a reviewed patch, updated audit evidence and a new complete package; distinguish verified results from remaining external checks. Publish or deploy only within the current task's authorization.
